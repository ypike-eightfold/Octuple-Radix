export interface LoadingOverlayProps {
    message?: string;
}
/**
 * Loading Overlay Component
 *
 * Displays a loading spinner with optional message over the table content.
 * Uses Radix Spinner for consistent styling.
 */
export declare function LoadingOverlay({ message }: LoadingOverlayProps): import("react/jsx-runtime").JSX.Element;
